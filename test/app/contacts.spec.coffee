# Backbone = require "backbone"
# Backbone.LocalStorage = require "backbone.localstorage"
# Contact = require "../../src/models/Contact"
# Account = require "../../src/models/Account"
# Contacts = require "../../src/collections/Contacts"
# {expect, assert, should, use} = require "chai"
# should()
# 
# describe "Contacts collection", ->
# 
#     contacts = new Contacts
# 
#     it "should be a backbone collection", ->
#         contacts.should.be.an.instanceof Contacts
#         contacts.should.be.an.instanceof Backbone.Collection
        
